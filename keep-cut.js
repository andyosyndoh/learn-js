function cutFirst(str) {
    return slice(str, 2);
};

function cutLast(str) {
    return slice(str, -2);
};

function cutFirstLast(str) {
    const length = str.length-2; 
    return slice(str, 2, 2);
};

function KeepFirst(str) {
    return slice(str, 0, );
};
